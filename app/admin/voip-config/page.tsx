import { VoipConfigClient } from './voip-config-client';

export const metadata = { title: 'VoIP.ms Config — HMU Admin' };

export default function VoipConfigPage() {
  return <VoipConfigClient />;
}
