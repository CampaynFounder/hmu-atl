// Driver Activity inbox — comments left on your profile + replies to you.
import { CommentActivityScreen } from '@/components/comments/CommentActivityScreen';

export default function DriverActivity() {
  return <CommentActivityScreen />;
}
